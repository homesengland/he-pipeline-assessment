import { EventEmitter } from '../../../../stencil-public-runtime';
import { RouterHistory } from '@stencil/router';
import { ActivityDefinition, ActivityModel, ConfigureComponentCustomButtonContext, ConnectionDefinition, ConnectionModel, WorkflowDefinition, WorkflowDefinitionVersion, WorkflowInstance, WorkflowModel, WorkflowTestActivityMessage } from '../../../../models';
import { ActivityStats } from '../../../../services';
import { ActivityContextMenuState, LayoutDirection, WorkflowDesignerMode } from '../../../designers/tree/elsa-designer-tree/models';
import { i18n } from 'i18next';
export declare class ElsaWorkflowDefinitionEditorScreen {
  workflowSaved: EventEmitter<WorkflowDefinition>;
  workflowDefinitionId: string;
  serverUrl: string;
  monacoLibPath: string;
  features: string;
  culture: string;
  basePath: string;
  serverFeatures: Array<string>;
  history: RouterHistory;
  workflowDefinition: WorkflowDefinition;
  workflowModel: WorkflowModel;
  publishing: boolean;
  unPublishing: boolean;
  unPublished: boolean;
  saving: boolean;
  saved: boolean;
  importing: boolean;
  imported: boolean;
  reverting: boolean;
  reverted: boolean;
  networkError: string;
  selectedActivityId?: string;
  workflowDesignerMode: WorkflowDesignerMode;
  workflowTestActivityMessages: Array<WorkflowTestActivityMessage>;
  workflowInstance?: WorkflowInstance;
  workflowInstanceId?: string;
  activityStats?: ActivityStats;
  layoutDirection: LayoutDirection;
  activityContextMenuState: ActivityContextMenuState;
  activityContextMenuTestState: ActivityContextMenuState;
  i18next: i18n;
  el: HTMLElement;
  designer: HTMLElsaDesignerTreeElement;
  configureComponentCustomButtonContext: ConfigureComponentCustomButtonContext;
  helpDialog: HTMLElsaModalDialogElement;
  activityContextMenu: HTMLDivElement;
  componentCustomButton: HTMLDivElement;
  confirmDialog: HTMLElsaConfirmDialogElement;
  getServerUrl(): Promise<string>;
  getWorkflowDefinitionId(): Promise<string>;
  exportWorkflow(): Promise<void>;
  importWorkflow(file: File): Promise<void>;
  workflowDefinitionIdChangedHandler(newValue: string): Promise<void>;
  serverUrlChangedHandler(newValue: string): Promise<void>;
  monacoLibPathChangedHandler(newValue: string): Promise<void>;
  workflowChangedHandler(event: CustomEvent<WorkflowModel>): Promise<void>;
  onWindowClicked(event: Event): void;
  componentWillLoad(): Promise<void>;
  componentDidLoad(): Promise<void>;
  componentDidRender(): void;
  connectedCallback(): void;
  disconnectedCallback(): void;
  configureComponentCustomButton(message: WorkflowTestActivityMessage): Promise<void>;
  t: (key: string) => string;
  loadActivityDescriptors(): Promise<void>;
  loadWorkflowStorageDescriptors(): Promise<void>;
  tryUpdateActivityInformation(activityId: string): Promise<void>;
  updateWorkflowDefinition(value: WorkflowDefinition): void;
  publishWorkflow(): Promise<void>;
  unpublishWorkflow(): Promise<void>;
  revertWorkflow(): Promise<void>;
  saveWorkflow(publish?: boolean): Promise<void>;
  saveWorkflowInternal(workflowModel?: WorkflowModel, publish?: boolean): Promise<void>;
  unpublishWorkflowInternal(): Promise<void>;
  revertWorkflowInternal(): Promise<void>;
  updateUrl(id: any): void;
  mapWorkflowModel(workflowDefinition: WorkflowDefinition): WorkflowModel;
  mapActivityModel(source: ActivityDefinition): ActivityModel;
  mapConnectionModel(source: ConnectionDefinition): ConnectionModel;
  handleContextMenuChange(state: ActivityContextMenuState): Promise<void>;
  handleContextMenuTestChange(x: number, y: number, shown: boolean, activity: ActivityModel): void;
  onShowWorkflowSettingsClick(): Promise<void>;
  onDeleteClicked(): Promise<void>;
  onPublishClicked(): Promise<void>;
  onUnPublishClicked(): Promise<void>;
  onRevertClicked(): Promise<void>;
  onExportClicked(): Promise<void>;
  onImportClicked(file: File): Promise<void>;
  onDeleteActivityClick(e: Event): Promise<void>;
  onEditActivityClick(e: Event): Promise<void>;
  onActivityContextMenuButtonClicked(e: CustomEvent<ActivityContextMenuState>): Promise<void>;
  onActivityContextMenuButtonTestClicked(e: CustomEvent<ActivityContextMenuState>): Promise<void>;
  onActivitySelected(e: CustomEvent<ActivityModel>): Promise<void>;
  onActivityDeselected(e: CustomEvent<ActivityModel>): Promise<void>;
  onTestActivityMessageReceived: (args: any) => Promise<void>;
  onRestartActivityButtonClick(): Promise<void>;
  private onUpdateWorkflowSettings;
  private onFlyoutPanelTabSelected;
  onUpdateActivity: (activity: ActivityModel) => void;
  private clearSubsequentWorkflowTestMessages;
  renderActivityStatsButton: (activity: ActivityModel) => string;
  renderMonacoEditorDialog(): any;
  render(): any;
  renderCanvas(): any;
  onComponentCustomButtonClick(message: WorkflowTestActivityMessage): Promise<void>;
  private canBeRestartedFromCurrentActivity;
  renderTestActivityMenu: () => any;
  renderActivityContextMenu(): any;
  renderActivityPicker(): any;
  renderActivityEditor(): any;
  renderWorkflowSettingsButton(): any;
  renderWorkflowHelpButton(): any;
  showHelpModal: () => Promise<void>;
  renderSavingIndicator(): any;
  renderNetworkError(): any;
  renderPublishButton(): any;
  private static createWorkflowDefinition;
  private renderWorkflowPanel;
  private renderTestPanel;
  private renderDesignerPanel;
  private renderVersionHistoryPanel;
  handleFeatureChange: (e: CustomEvent<string>) => void;
  handleFeatureStatusChange: (e: CustomEvent<string>) => void;
  onVersionSelected: (e: CustomEvent<WorkflowDefinitionVersion>) => Promise<void>;
  onDeleteVersionClicked: (e: CustomEvent<WorkflowDefinitionVersion>) => Promise<void>;
  onRevertVersionClicked: (e: CustomEvent<WorkflowDefinitionVersion>) => Promise<void>;
}
